import 'package:flutter/material.dart';

class AddLocationsScreen extends StatelessWidget {
  const AddLocationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("location page"),
      ),
    );
  }
}
